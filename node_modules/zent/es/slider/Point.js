import * as React from 'react';
import cx from 'classnames';
function SliderPoint(props) {
    var value = props.value, position = props.position, disabled = props.disabled, active = props.active;
    return (React.createElement("div", { className: cx('zent-slider-tooltip', {
            'zent-slider-tooltip-active': active,
        }), style: {
            left: position,
        } },
        React.createElement("div", { className: cx('zent-slider-point', {
                'zent-slider-point-disabled': disabled,
            }) }),
        React.createElement("div", { className: "zent-slider-tooltip-content" }, value)));
}
export default SliderPoint;
