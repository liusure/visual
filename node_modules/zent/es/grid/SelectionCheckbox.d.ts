import { PureComponent } from 'react';
import { ICheckboxProps } from '../checkbox';
import Store from './Store';
interface IGridSelctionCheckboxProps {
    disabled?: boolean;
    rowIndex: number | string;
    store: Store;
    onChange: ICheckboxProps<unknown>['onChange'];
}
interface IGridSelctionCheckboxState {
    checked: boolean;
}
declare class SelectionCheckbox extends PureComponent<IGridSelctionCheckboxProps, IGridSelctionCheckboxState> {
    constructor(props: IGridSelctionCheckboxProps);
    unsubscribe?: () => void;
    subscribe: () => void;
    getCheckState: (props: IGridSelctionCheckboxProps) => boolean;
    componentDidMount(): void;
    componentWillReceiveProps(nextProps: IGridSelctionCheckboxProps, nextState: IGridSelctionCheckboxState): void;
    componentWillUnmount(): void;
    render(): JSX.Element;
}
export default SelectionCheckbox;
