import { __assign } from "tslib";
import forOwn from 'lodash-es/forOwn';
import cloneDeep from 'lodash-es/cloneDeep';
var mergeClasses = function (classes, activeNames) {
    if (activeNames === void 0) { activeNames = []; }
    var styles = (classes.default && cloneDeep(classes.default)) || {};
    activeNames.map(function (name) {
        var toMerge = classes[name];
        if (toMerge) {
            forOwn(toMerge, function (value, key) {
                if (!styles[key]) {
                    styles[key] = {};
                }
                styles[key] = __assign(__assign({}, styles[key]), toMerge[key]);
            });
        }
        return name;
    });
    return styles;
};
export default mergeClasses;
