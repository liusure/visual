import * as React from 'react';
import { Omit } from 'utility-types';
import { IFormComponentProps } from '../shared';
import { ISelectProps } from '../../select';
export declare type IFormSelectFieldProps<T> = Omit<IFormComponentProps<T | T[], Omit<ISelectProps, 'value' | 'tags' | 'onChange'>>, 'normalize' | 'format'> & {
    tags?: boolean;
    data: any[];
};
export declare const FormSelectField: React.FunctionComponent<IFormSelectFieldProps<any>>;
