import { success, warn, error, clear, config } from './Notify';
export var Notify = {
    success: success,
    warn: warn,
    error: error,
    clear: clear,
    config: config,
};
export default Notify;
