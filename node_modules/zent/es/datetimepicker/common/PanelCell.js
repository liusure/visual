import { __extends } from "tslib";
import * as React from 'react';
import { PureComponent } from 'react';
import { noop } from '../constants';
var PanelCell = (function (_super) {
    __extends(PanelCell, _super);
    function PanelCell() {
        var _this = _super !== null && _super.apply(this, arguments) || this;
        _this.onClickCell = function (cell) {
            !cell.isDisabled && _this.props.onSelect(cell.value);
        };
        return _this;
    }
    PanelCell.prototype.getTbody = function () {
        var _this = this;
        var _a = this.props, cells = _a.cells, onHover = _a.onHover;
        return cells.map(function (row, i) {
            var tds = row.map(function (col, j) {
                return (React.createElement("li", { className: "grid-cell", role: "gridcell", key: j },
                    React.createElement("div", { onClick: function () { return _this.onClickCell(col); }, onMouseOver: function () { return onHover(col.value); }, className: col.className, title: col.title }, col.text)));
            });
            return (React.createElement("ul", { className: "panel-table__row", role: "row", key: i }, tds));
        });
    };
    PanelCell.prototype.render = function () {
        return React.createElement("div", null, this.getTbody());
    };
    PanelCell.defaultProps = {
        onHover: noop,
    };
    return PanelCell;
}(PureComponent));
export default PanelCell;
