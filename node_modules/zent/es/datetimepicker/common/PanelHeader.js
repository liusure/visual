import * as React from 'react';
import Icon from '../../icon';
var PanelHeader = function (props) {
    var onClickTitle = props.onClickTitle, next = props.next, prev = props.prev, showPrev = props.showPrev, showNext = props.showNext, title = props.title;
    return (React.createElement("div", { className: "panel__header" },
        showPrev ? (React.createElement("span", { className: "link--prev", onClick: prev },
            React.createElement(Icon, { type: "right" }))) : null,
        React.createElement("span", { className: "panel__title", onClick: onClickTitle }, title),
        showNext ? (React.createElement("span", { className: "link--next", onClick: next },
            React.createElement(Icon, { type: "right" }))) : null));
};
PanelHeader.defaultProps = {
    showPrev: true,
    showNext: true,
};
export default PanelHeader;
