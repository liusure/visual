import * as React from 'react';
import Button from '../../button';
import { noop } from '../constants';
var PanelFooter = function (props) {
    var buttonText = props.buttonText, errorText = props.errorText, linkCls = props.linkCls, linkText = props.linkText, onClickButton = props.onClickButton, onClickLink = props.onClickLink, showError = props.showError, showLink = props.showLink;
    return (React.createElement("div", { className: "panel__footer" },
        showLink && (React.createElement("a", { className: linkCls, onClick: onClickLink }, linkText)),
        showError && React.createElement("span", { className: "error-tips" }, errorText),
        React.createElement(Button, { className: "btn--confirm", type: "primary", onClick: onClickButton }, buttonText)));
};
PanelFooter.defaultProps = {
    onClickLink: noop,
    onClickButton: noop,
    showLink: true,
    showError: false,
    errorText: '',
};
export default PanelFooter;
