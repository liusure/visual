import { __extends } from "tslib";
import * as React from 'react';
import { PureComponent } from 'react';
import { noop } from '../constants';
var TimeCell = (function (_super) {
    __extends(TimeCell, _super);
    function TimeCell() {
        var _this = _super !== null && _super.apply(this, arguments) || this;
        _this.onClickCell = function (cell) {
            !cell.isDisabled && _this.props.onSelect(cell.value);
        };
        return _this;
    }
    TimeCell.prototype.getTbody = function () {
        var _this = this;
        var cells = this.props.cells;
        return cells.map(function (row, i) {
            var tds = row.map(function (col, j) {
                return (React.createElement("li", { className: "grid-cell", role: "gridcell", key: j },
                    React.createElement("span", { onClick: function () { return _this.onClickCell(col); }, className: col.className, title: "" + col.value }, col.text)));
            });
            return (React.createElement("ul", { className: "panel-table__row", role: "row", key: i }, tds));
        });
    };
    TimeCell.prototype.render = function () {
        return React.createElement("div", { className: "panel-table__list" }, this.getTbody());
    };
    TimeCell.defaultProps = {
        onSelect: noop,
    };
    return TimeCell;
}(PureComponent));
export default TimeCell;
