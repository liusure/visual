import { __extends } from "tslib";
import * as React from 'react';
import { I18nReceiver as Receiver } from '../../../i18n';
import { NumberInput } from '../../../number-input';
import BasePageJumper from './BasePageJumper';
var INPUT_WIDTH = 56;
var PageJumper = (function (_super) {
    __extends(PageJumper, _super);
    function PageJumper() {
        return _super !== null && _super.apply(this, arguments) || this;
    }
    PageJumper.prototype.render = function () {
        var value = this.state.value;
        var input = (React.createElement(NumberInput, { value: value, onInput: this.onChange, onPressEnter: this.onConfirm, width: INPUT_WIDTH, min: 1, decimal: 0 }));
        return (React.createElement(Receiver, { componentName: "Pagination" }, function (i18n) {
            if (i18n.mark === 'zh-CN') {
                return (React.createElement("div", { className: "zent-pagination-page-jumper" },
                    i18n.jump,
                    input,
                    i18n.page));
            }
            return (React.createElement("div", { className: "zent-pagination-page-jumper" },
                i18n.jump,
                " ",
                i18n.page,
                input));
        }));
    };
    PageJumper.prototype.handleJump = function (pageNumber) {
        this.props.onJump(pageNumber);
        this.setState({
            value: '',
        });
    };
    return PageJumper;
}(BasePageJumper));
export { PageJumper };
export default PageJumper;
