import * as React from 'react';
import { DEFAULT_WIDTH } from '../constants';
import ProgressInfo from './ProgressInfo';
var LineProgress = function (props) {
    var format = props.format, width = props.width, percent = props.percent, showInfo = props.showInfo, strokeWidth = props.strokeWidth, bgColor = props.bgColor, color = props.color, state = props.state;
    var progressWidth = width || DEFAULT_WIDTH.LINE;
    var progressInfo = showInfo && (React.createElement("div", { className: "zent-progress-info", style: { color: color } },
        React.createElement(ProgressInfo, { type: "line", percent: percent, format: format, state: state })));
    return (React.createElement("div", { className: "zent-progress-container" },
        React.createElement("div", { className: "zent-progress-wrapper", style: {
                background: bgColor,
                width: progressWidth,
                height: strokeWidth,
            } },
            React.createElement("div", { className: "zent-progress-inner", style: {
                    background: color,
                    width: percent + "%",
                    height: strokeWidth,
                } })),
        progressInfo));
};
export default LineProgress;
