import * as React from 'react';
import cn from 'classnames';
function TabPanel(props) {
    var actived = props.actived, className = props.className, unmountOnHide = props.unmountOnHide, children = props.children;
    if (!actived && unmountOnHide) {
        return null;
    }
    var displayStyle = actived ? {} : { display: 'none' };
    var panelCls = cn('zent-tabs-panel', className);
    return (React.createElement("div", { role: "tabpanel", style: displayStyle, className: panelCls }, children));
}
export default TabPanel;
