import _classCallCheck from 'babel-runtime/helpers/classCallCheck';
import _possibleConstructorReturn from 'babel-runtime/helpers/possibleConstructorReturn';
import _inherits from 'babel-runtime/helpers/inherits';
import _isFunction from 'lodash/isFunction';

var _class, _temp;

import React, { PureComponent } from 'react';
import { findDOMNode } from 'react-dom';

import scroll from 'zent/es/utils/scroll';

import offset from '../utils/offset';

function scrollNodeToTop(node, offsets) {
  var pos = offset(node);
  var top = _isFunction(offsets.top) ? offsets.top(pos.top) : pos.top + offsets.top;
  var left = _isFunction(offsets.left) ? offsets.left(pos.left) : pos.left + offsets.left;
  scroll(document.body, left, top);
}

var DesignPreviewItem = (_temp = _class = function (_PureComponent) {
  _inherits(DesignPreviewItem, _PureComponent);

  function DesignPreviewItem() {
    _classCallCheck(this, DesignPreviewItem);

    return _possibleConstructorReturn(this, _PureComponent.apply(this, arguments));
  }

  DesignPreviewItem.prototype.render = function render() {
    var _props = this.props,
        children = _props.children,
        prefix = _props.prefix;


    return React.createElement(
      'div',
      { className: prefix + '-design-preview-item' },
      children
    );
  };

  DesignPreviewItem.prototype.scrollTop = function scrollTop(offsets) {
    var node = findDOMNode(this);
    scrollNodeToTop(node, offsets);
  };

  return DesignPreviewItem;
}(PureComponent), _class.defaultProps = {
  prefix: 'zent'
}, _temp);
export { DesignPreviewItem as default };